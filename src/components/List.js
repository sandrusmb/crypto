import React from 'react';
import Detail from './Detail';

const List = (props) => {
    return (
        <div>
            <Detail />
            <Detail />
        </div>
    )
}

export default List;